// TYPES
// array, object, string
// renderer
const plugin = {
  'organizations': 'array',
  'photos': 'array',
  'digitalFootprint.scores': 'array',
  'digitalFootprint.topics': 'array',
  'contactInfo.websites': 'array',
  'writingInformation.beats': 'array',
  'writingInformation.occasionalBeats': 'array',
};

export default plugin;
